require 'spec_helper'

describe MessengerPlatform do
  it 'has a version number' do
    expect(MessengerPlatform::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
